# resume-ats-docx-gen
A resume creation app that lets users that input the JSON file with specific fields generate a formatted resume file in docx that is good for ATS parsing. Intended to be used by AI app in future project.
